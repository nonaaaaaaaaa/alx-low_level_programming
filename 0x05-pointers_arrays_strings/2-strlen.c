#include "main.h"
/**
 * _strlen - function to return length of sting
 * @s: input string
 * Return: length of string
 *
 */
int _strlen(char *s)
{
	int i;

	for (i = 0; s[i] != '\0'; i++)
	{
		return (i);
	}
}
